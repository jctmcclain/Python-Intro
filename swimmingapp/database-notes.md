# Creating the tables 

```sql
CREATE TABLE swimmers(id integer autoincrement primary key, 
firstname varchar(40), 
lastname varchar(40),
email varchar(100),
year varchar(20));

```
# Joining tables 
